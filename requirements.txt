# Kept for reference only. The supported install path is conda:
#
#     conda env create --prefix ./envs/dikat -f environment.yml
#     conda activate ./envs/dikat
#
# See environment.yml for why (PyTorch + PyG + RDKit from one channel).
# Recommended Python: 3.10-3.12.

numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
optuna>=3.4
torch>=2.0
torch-geometric>=2.5
rdkit>=2023.09
pillow>=10.0
